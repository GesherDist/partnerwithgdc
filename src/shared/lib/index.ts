/**
 * Library Index
 *
 * Central export point for shared libraries.
 */

// Utilities
export { cn } from './utils';

// Database - Use the Supabase database client
// import { db } from '@/shared/lib/supabase/database';

// Supabase - Note: Import specific clients from their modules
// Browser: import { createBrowserClient } from '@/shared/lib/supabase/client';
// Server: import { createServerClient } from '@/shared/lib/supabase/server';
// Admin: import { createAdminClient } from '@/shared/lib/supabase/admin';
