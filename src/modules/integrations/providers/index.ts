/**
 * Integration Providers
 *
 * Re-export provider instances only to avoid naming conflicts.
 * For specific exports, import directly from the provider module.
 */

// Accounting Provider instances
export { quickBooksProvider } from './accounting';

// CRM Provider instances
export { pipedriveProvider } from './crm';
